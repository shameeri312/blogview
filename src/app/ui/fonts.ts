import { Red_Hat_Display, Raleway } from "next/font/google";

export const mont = Red_Hat_Display({
  subsets: ["latin"],
  weight: ["400", "500", "700"],
});
export const satisfy = Raleway({
  subsets: ["latin"],
  weight: ["400"],
});
